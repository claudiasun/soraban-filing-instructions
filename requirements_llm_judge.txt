# Core dependencies for LLM judge
pandas>=2.0.0
pyarrow>=12.0.0
pillow>=10.0.0
pdf2image>=1.16.0
python-dotenv>=1.0.0  # For loading .env files

# LLM providers (install at least one)
openai>=1.0.0        # For GPT-4o
anthropic>=0.18.0    # For Claude 3.5 Sonnet
google-generativeai>=0.8.5  # For Gemini

# Optional: For bounding box extraction
paddlepaddle>=2.5.0
paddleocr>=2.7.0
# Note: You also need poppler-utils installed on your system:
# macOS: brew install poppler
# Ubuntu: sudo apt-get install poppler-utils

